import React from 'react';
import styled from 'styled-components';

const WrapperStyle = styled.div`
    text-align: center;
    font-size: 50px;
`;

const Main: React.FC = () => {
    return (
        <WrapperStyle>
            TEST
        </WrapperStyle>
    );
};

export default Main;
